# 智能识别准确率测试大纲

> 国家重点研发计划"诊疗装备与生物医用材料"重点专项
>
> - 所属项目：青少年近视防控功能镜片关键参数计量技术研究（项目编号：2024YFC2419500）
> - 所属课题：离焦微结构镜片磨损程度检测及关键参数评估研究（课题编号：2024YFC2419504）
> - 测试对象：智能磨损识别算法 —— 磨损识别准确率
> - 编制单位：课题承担单位
> - 编制时间：二〇二六年 四 月

本大纲保持"磨损识别准确率"作为统一的测试指标名称，以单图像一致性判定 \(\delta_i\) 为基础进行统计。算法版本与数据集版本的后续更新不改变本大纲的适用性。

## 1 主题内容和适用范围

### 1.1 主题内容

本大纲用于指导第三方测试机构对"离焦微结构镜片磨损智能识别任务"中"磨损识别准确率"指标开展测试。大纲规定了测试目的、测试依据、测试组织管理、测试流程、测试用例和测试对象、测试环境、测试方法和步骤以及测试留痕要求，作为委托测试和结果判定的依据性文件。

本大纲仅围绕课题任务书中"2.1 磨损识别准确率"相关内容编制，不扩展至其他功能指标、性能指标或研究性分析内容。

### 1.2 适用范围

本大纲适用于项目"青少年近视防控功能镜片关键参数计量技术研究"下，课题"离焦微结构镜片磨损程度检测及关键参数评估研究"中智能识别任务形成的软件产品或算法系统的第三方测试。

本大纲适用于对测试图像中磨损识别结果进行准确率测试。智能磨损识别算法只要能够针对磨损镜片图像给出可与基准结果进行对应比较的识别结果，均可依据本大纲开展测试。

本大纲规定统一的测试原则、统计要求和结果形成方式。测试所采用的图像数据、基准结果、统计规则和判定阈值，以测试实施阶段经委托测试文件确认的版本为准。算法版本、数据集版本及其更新不改变本大纲的适用性。

## 2 测试目的

测试目的是验证智能磨损识别算法在规定测试图像集上对镜片磨损目标的识别结果，与人工确认并冻结的基准结果之间的一致程度是否达到委托测试文件规定的要求，并形成可供委托方、测试机构及验收使用的客观测试结论。

本测试仅针对"磨损识别准确率"指标开展，不涉及缺陷机理分析、缺陷分级评估、复杂区域成因解释及其他扩展性研究内容。

## 3 测试依据和引用文件

- `GB/T 25000.51-2016`《系统与软件工程 系统与软件质量要求与评价（SQuaRE） 第 51 部分：就绪可用软件产品（RUSP）的质量要求和测试细则》；
- `GB/T 38634`（系列）《系统与软件工程 软件测试》系列标准中与测试过程、测试用例设计和测试执行相关的条款；
- `GB/T 41864-2022`《信息技术 计算机视觉 术语》；
- 《青少年近视防控功能镜片关键参数计量技术研究》项目任务书（项目编号：2024YFC2419500）；
- 《离焦微结构镜片磨损程度检测及关键参数评估研究》课题任务书（课题编号：2024YFC2419504）。

当本大纲与具体委托测试文件、合同要求或任务书明确条款存在差异时，以后者的明确要求为准。

## 4 测试组织管理和流程

### 4.1 测试组织管理

测试组织管理应体现独立、客观、可追溯和可重复执行的原则。

- 委托方负责提交送测算法、测试图像集、基准结果和必要说明；
- 测试机构负责实施测试、核验结果并形成测试结论；
- 技术配合方负责部署支持和问题说明，不参与正式结果判定。

测试资料、运行条件和结果记录应能够相互对应，便于复核。

### 4.2 测试流程

测试一般按以下顺序开展：

1. 确认送测资料；
2. 核验测试环境；
3. 执行正式测试并保存结果；
4. 核验结果，必要时进行重复性核验；
5. 归档自动生成的日志、截图、溯源信息和测试报告。

若出现算法无法运行、数据读取失败或结果无法复现等情况，应记录原因和处理情况，并在测试报告中说明。

## 5 测试用例和测试对象

### 5.1 磨损镜片图像

本大纲以单张磨损镜片图像作为基本测试单元。正式测试使用的图像应来自经确认的测试图像集，并与基准结果一一对应。

图像清单宜采用结构化文件，记录图像标识、文件路径和对应基准结果版本。正式测试前应完成图像、基准结果和图像清单的一致性核对。

当前送测算法所对应的完整测试集清单文件为：

- `cnas_test/manifests/full_dataset_v1.json`

完整测试集统计口径：离焦微结构镜片磨损识别数据集包含 `10621` 个标准化图像样本、`90325` 个标注缺陷框，按类别分布为 `scratch=58032`、`spot=17475`、`critical=14818`。样本由离焦微结构镜片磨损显微图像及人工复核的目标框标注按固定尺度和坐标映射规则生成，样本图像尺寸为 `640 × 640` 像素。

### 5.2 智能磨损识别算法

本次接受测试的对象为智能磨损识别算法。算法应能够读取规定格式的磨损镜片图像，并输出可用于统计和核验的识别结果。送测时应明确：

- 算法名称、版本号及交付形态；
- 算法运行所需的硬件、操作系统、运行时和依赖；
- 算法的输入图像格式和输出结果格式；
- 算法默认运行参数与可调参数说明；
- 算法执行命令与日志输出位置。

当前送测算法标识：

- 被测软件名称：镜片磨损智能识别算法
- 测试版本：`LWIA-Det v1.0.0`
- 版本备注：内部训练注释 `B2_nwd_only_phase3e`
- 模型描述：多尺度深度目标检测网络，结合小目标定位优化策略
- 标准执行命令：`uv run python -m cnas_test.runner.run_eval`
- 模型权重路径：`output/experiments/phase3e/detection_training/b2_nwd_only_phase3e/weights/best.pt`

## 6 性能指标测试

### 6.1 测试说明和要求

本次测试项目为"磨损识别准确率测试"。测试时，以识别结果与基准结果的一致程度作为评价依据。

| 测试项目 | 测试目的 | 结果形式 | 评价指标 |
| --- | --- | --- | --- |
| 磨损识别准确率 | 验证识别结果与基准结果的一致程度 | 逐图像比较与统计 | 磨损识别准确率 |

同一次测试应采用统一的测试图像、基准结果、统计规则和判定方式。正式测试前，应登记测试图像集版本、基准结果版本、图像总数 \(N\) 和异常样本处理方式。

如需进行重复性核验，应在相同条件下进行，两次结果之间的主指标差值应满足委托测试文件规定的一致性要求。

### 6.2 测试环境及条件

测试环境应满足算法正常运行的要求，并具备稳定、可复现和可重复执行测试的条件。正式测试前，应确认输入输出路径、配置文件和访问权限满足测试要求，并保证结果和日志能够完整保存。正式测试与重复性核验应在一致的运行条件下完成。

测试环境的具体执行方式（本地或远程桌面）按附录 A《测试执行说明（远程与本地）》执行。

### 6.3 测试方法和步骤

#### 6.3.1 指标统计公式

设纳入本次统计的图像总数为 \(N\)。对第 \(i\) 张图像，记算法输出结果为 \(R_i\)，对应基准结果为 \(G_i\)，其单张图像判定结果为 \(\delta_i\)。

当第 \(i\) 张图像的识别结果满足本次测试确认的判定规则时：

\[
\delta_i = 1
\]

当第 \(i\) 张图像的识别结果不满足本次测试确认的判定规则时：

\[
\delta_i = 0
\]

则总体磨损识别准确率 \(A\) 按下式计算：

\[
A = \frac{1}{N}\sum_{i=1}^{N}\delta_i \times 100\%
\]

符号说明：

- \(N\)：纳入统计的图像总数；
- \(i\)：图像序号；
- \(R_i\)：第 \(i\) 张图像的算法输出结果；
- \(G_i\)：第 \(i\) 张图像对应的基准结果；
- \(\delta_i\)：第 \(i\) 张图像的单张图像判定结果；
- \(A\)：总体磨损识别准确率。

当送测算法采用目标级检测输出时，按附录 B 规定的目标级评价方式（`mAP50`）实现 \(A\)。当送测算法采用区域级输出时，按附录 B 规定的区域级评价方式实现 \(A\)。

#### 6.3.2 一致性判定规则

\(\delta_i\) 的具体定义应在正式测试前明确，并在同一次测试中保持一致。判定规则宜明确比较方式、对应阈值以及异常样本处理原则。

#### 6.3.3 测试实施步骤

1. 核对测试图像集、基准结果、图像清单以及算法版本和配置文件；
2. 确认测试环境满足运行要求，并保持测试条件一致；
3. 对全部图像执行正式测试，保存原始结果、统计结果和日志；
4. 依据判定规则逐图像计算 \(\delta_i\)，并按公式计算总体磨损识别准确率 \(A\)；
5. 必要时在相同条件下进行重复性核验，并比较两次结果差值；
6. 归档自动生成的日志、截图、溯源文件和测试报告。

#### 6.3.4 结果输出物清单

正式测试在 `cnas_test/outputs/YYYYMMDD_HHMMSS/` 形式的时间命名目录下生成以下交付物（详见 `delivery_manifest.json`）：

- `dataset/`：数据集 YAML 与样本清单；
- `metrics/cnas_eval_results.json`：正式统计指标结果；
- `plots/ultralytics_output/`：混淆矩阵、PR/F1/P/R 曲线、典型预测样例；
- `screenshots/`：测试过程关键节点截图（启动横幅、结果汇总、AP 条形图）；
- `provenance/provenance.json`：git commit、权重 SHA256、测试集 SHA256、依赖冻结、GPU 等溯源信息；
- `reports/cnas_test_report.md` / `.html` / `.docx`：三种格式的正式测试报告；
- `delivery_manifest.json`：交付物总清单。

#### 6.3.5 判定原则

本大纲规定统一的测试对象、测试内容、测试方法和统计公式框架，不在正文中固化具体达标阈值。是否通过测试，应以磨损识别准确率 \(A\) 与任务书、委托测试文件或测试实施细则中规定的阈值比较后确定。

## 附录 A 测试执行说明（远程与本地）

详见同目录文件：[测试执行说明_远程与本地_当前版.md](./测试执行说明_远程与本地_当前版.md)。

## 附录 B 当前送测算法的评价方式实现

### B.1 目标级评价方式（当前送测算法适用）

对每一个识别类别，在 `IoU = 0.5` 的条件下，将预测结果按置信度从高到低排序，并与对应类别的基准结果进行一对一匹配。

\[
\text{Precision}(k) = \frac{TP(k)}{TP(k) + FP(k)}, \qquad \text{Recall}(k) = \frac{TP(k)}{N_{gt}}
\]

该类别的平均准确率 `AP50` 为在 `IoU = 0.5` 条件下精确率-召回率曲线下面积：

\[
AP50 = \int_{0}^{1} P(R)\,dR \quad (\text{IoU} = 0.5)
\]

对全部参与评价的类别分别计算 `AP50` 后取算术平均，得到本次测试的主指标 `mAP50`：

\[
mAP50 = \frac{1}{C} \sum_{i=1}^{C} AP50_i
\]

参数取值（与正式执行一致）：

| 参数 | 取值 | 说明 |
| --- | ---: | --- |
| 置信度阈值（评测） | `0.001` | 低阈值以画全 PR 曲线，不影响 AP 计算 |
| NMS IoU | `0.6` | `ultralytics.val` 的去重叠 IoU，不参与匹配 |
| AP 匹配 IoU | `0.5` | 与 `mAP50` 命名一致 |

### B.2 区域级评价方式（后续送测算法适用）

当送测算法采用区域级（像素级语义/实例分割）输出时，单图像判定 \(\delta_i\) 由该图像的区域级一致性指标（如 `mIoU`、`Dice` 或像素级匹配率）与委托测试文件规定的阈值比较得出。本大纲不再固化具体阈值。
